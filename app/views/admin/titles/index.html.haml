%h1 Listing titles

%table
  %tr
    %th Nama
    %th Skim
    %th
    %th
    %th

  - @titles.each do |title|
    %tr
      %td= title.nama
      %td= title.skim
      %td= link_to 'Show', [:admin, title]
      %td= link_to 'Edit', edit_admin_title_path(title)
      %td= link_to 'Destroy', [:admin, title], :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Title', new_admin_title_path
